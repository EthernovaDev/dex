import { Currency, Price } from '@im33357/uniswap-v2-sdk'

/**
 * Returns the price in USDC of the input currency
 * @param currency currency to compute the USDC price of
 */
export default function useUSDCPrice(_currency?: Currency): Price | undefined {
  return undefined
}
